[project]
name = "processcodenew"
version = "0.1.0"
description = "Existing processing code from Xavi, retrofit and wrapped for HAMMR-HD."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.5",
    "scipy>=1.17.1",
    "tables>=3.11.1",
]
